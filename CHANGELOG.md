# 更新日志

## 2.0.18（2026-08-03）

- 修复任务页定时配置在编辑期间被自动轮询返回的旧数据覆盖的问题；未保存修改会保留在表单中，保存成功后才重新同步服务端配置。
- 保存请求进行期间继续修改表单时，保留后续输入，避免较早请求的返回结果覆盖新的未保存配置。

## 2.0.17（2026-08-02）

- 定时同步新增“单次定时发布上限”配置；填 `0` 表示不限制，填入正整数后本轮只处理对应数量的候选商品，完成后正常结束，下一次定时任务继续处理剩余商品。
- 该上限仅应用于定时触发的同步，手动执行“全部商品 / 未发布商品 / 已更新商品”不受影响；同步任务日志记录本轮实际使用的上限值。
- SQLite 数据库结构版本升级至 `14`，补充调度配置持久化、管理接口和候选数量限制回归测试。

## 2.0.16（2026-07-31）

- 商品配置新增“默认库存”，默认值为 `999`，支持保存 `1–999999` 的整数并在模板预览中展示。
- 同步素材时会将默认库存传给闲鱼素材接口，库存被修改后会使已有素材进入待更新状态；已发布商品不会因此重新上架。
- 闲鱼同步配置接口新增 `default_stock` 字段，并补充库存持久化、参数校验和素材同步负载回归测试。

## 2.0.15（2026-07-31）

- 批量自选同步入口迁移至“任务”模块的“手动操作”区域，游戏数据页恢复为数据查看、筛选、定价和单商品同步。
- “自选有效游戏”弹窗只加载同时具备有效图片链接和下载资源的游戏，支持搜索、分页、跨页累计勾选、清空选择和启动同步。
- 游戏列表接口新增 `validOnly=true` 过滤参数，确保任务模块的选择范围与同步数据池一致；服务端仍会在提交时再次校验。
- 补充有效游戏筛选回归测试，完整测试保持 48 项。

## 2.0.14（2026-07-31）

- 游戏数据页新增有效游戏批量选择：只有同时具备有效图片链接和下载资源的游戏可勾选，可跨页累计选择后发起同步。
- 新增 `POST /api/games/sync-selected`，服务端会去重并重新校验所有游戏的有效性，拒绝不存在或缺少图片、下载资源的记录。
- 单商品同步入口同步补充有效数据校验；游戏列表与详情接口新增 `isValid` 字段，页面明确展示“可同步”或不可同步原因。
- 新增批量自选同步和无效游戏拒绝回归测试，完整测试保持 48 项。

## 2.0.13（2026-07-31）

- 任务页面新增不可恢复的“终止当前任务”按钮；保留“暂停/恢复”用于临时挂起，终止接口会等待当前任务真正退出后再返回非活动状态。
- 终止同步时会取消正在进行的闲鱼 HTTP 请求、封面下载和轮询等待；终止采集时会关闭 Playwright 浏览器并取消退避等待，避免任务 Promise 长期保持 `active`。
- 未提交发布批次的同步任务终止后记为“已中断”；已经提交但尚未确认结果的批次继续记为“待确认”，避免自动重发造成重复商品。
- 新增终止状态机、同步退出、HTTP/封面取消和管理接口回归测试，完整测试增至 48 项。

## 2.0.12（2026-07-31）

- 素材导入与商品发布改为两条独立异步流水线：发布线程发现已有可发布素材后立即提交，不再等待累计 20 个，也不再使用“尾数单独成批”规则。
- 任务页面新增“每批发布商品数”配置，支持 `1–20`；配置通过管理接口写入 SQLite，并同时用于手动同步和定时同步。
- 批量发布仍保持单批串行确认，单批实际数量取当前已就绪队列与页面配置上限的较小值；素材导入继续按页面设置的 `1–12` 路并行运行。
- 数据库结构版本升级到 `12`，补充双流水线交错执行、可配置批量上限、失败续批和配置持久化回归测试。

## 2.0.11（2026-07-31）

- 商品发布恢复使用闲鱼批量发布接口：每累计 20 个可发布素材提交一批，尾数单独成批，前一批完成并回写结果后才提交下一批，不再使用 4 路单商品发布。
- 任务页面移除“商品发布并行数”，改为展示固定的“每批最多 20 件，逐批执行”；素材导入并行数仍可独立配置为 `1–12`。
- 批次状态默认每 10 秒查询，状态变化或持续等待满 1 分钟时写入详细日志；明确参数失败跳过当前批并继续，结果未知时停止后续批次，避免重复发布。
- 批次成功后逐商品回写闲鱼商品编号和链接，并继续关联卡券 ID `6`；恢复 `SYNC_POLL_INTERVAL_MS` 和 `SYNC_BATCH_TIMEOUT_MS` 部署配置。
- 新增 `20+尾数` 分批、明确失败续批、未知结果停止、已有素材并入同批及页面固定批次大小回归测试，完整测试增至 44 项。

## 2.0.10（2026-07-30）

- 新增 GitHub Actions 生产发布流水线：版本标签或手动触发后构建 `linux/amd64` 镜像、推送 GHCR，并通过受限 SSH 密钥连接服务器部署。
- 服务器部署脚本会拒绝覆盖正在运行的采集或同步任务，部署前使用 SQLite `VACUUM INTO` 生成一致性备份，部署后校验容器健康、应用版本和公网健康接口。
- 公开仓库前使用 Gitleaks 扫描当前内容及 58 个历史提交，未发现密钥、Cookie、数据库或私钥泄露。

## 2.0.9（2026-07-30）

- 任务控制由“等待安全检查点中断”改为立即暂停：暂停后并发队列停止领取新商品，外部请求返回后也会先等待恢复，不再继续写入进度或进入下一处理步骤。
- 任务页面新增详情采集、素材导入和单商品发布三项并行数配置，支持 `1–12`，保存后写入 SQLite 并用于后续手动及定时任务。
- 新增 `/api/tasks/{crawl|sync}/pause` 接口，同时保留旧 `interrupt` 路径兼容；按钮文案、loading 和 toast 统一改为暂停语义。
- SQLite 数据库升级至版本 11，为单例任务设置增加三项并行数字段；新增自定义并行数和暂停停止出队回归测试，完整测试增至 43 项。

## 2.0.8（2026-07-30）

- 闲鱼同步改为素材导入与商品发布两条独立异步队列，两侧各保持 4 个并行任务；单件素材就绪后立即进入发布队列，不再等待 20 条成组，也不再调用批量发布接口。
- 商品发布改用同步单商品接口，成功后直接回写闲鱼商品编号并关联卡券 ID `6`；单件失败或结果未知只记录并跳过当前商品，不阻塞后续任务。
- 单商品发布请求执行期间每 1 分钟写入一条持久化心跳日志，日志弹窗可持续查看当前商品、请求编号和累计等待时间。
- 已有素材且内容哈希未变化时跳过素材上传，但单商品发布仍复用 Gamer520 持久化封面；内容哈希变化时继续更新素材库。
- 任务页“中断当前任务”和“恢复任务”按钮新增独立 loading 状态，并在操作成功或失败后展示明确 toast。
- 删除已不再使用的批量发布轮询与超时环境变量，新增单商品接口和双队列四并发回归测试。

## 2.0.7（2026-07-30）

- 采集和同步任务新增持久化逐步操作日志，记录任务启动、账号校验、列表与来源时间读取、详情采集、封面缓存、素材写入、批量发布、商品编号核对、卡券关联及最终结果；当前任务和历史任务均可打开日志弹窗，运行时每 2 秒自动刷新。
- 当前同步进度改为三色分段展示：本轮成功为绿色、已有数据跳过为黄色、失败为红色；采集任务使用相同颜色语义展示详情成功、来源时间未变化跳过和失败数量。
- 商品模板预览改用数据库内真实有效游戏和同源缓存封面，不再引用不存在的示例图片；无有效数据或图片加载失败时显示明确提示。
- 闲鱼服务 API Key 支持在后台验证并保存，刷新后只返回脱敏值；配置立即用于账号读取和后续同步。
- Gamer520 下载接口 API Key 支持生成多个独立 Key、明文复制和逐个删除，存量环境变量 Key 会自动迁移为默认 Key。
- SQLite 数据库升级至版本 10，新增任务操作日志、服务凭据和 Gamer520 下载接口 Key 表；完整测试增至 41 项。

## 2.0.6（2026-07-30）

- 修复发布进度错误复用素材跳过数量的问题：发布当前数量只统计任务范围内既有发布成功记录和本轮真实发布成功记录，素材已同步、同名跳过或素材失败均不再算作已发布。
- 已有有效 `material_id` 但尚无闲鱼商品编号的游戏不再从同步候选中排除；任务会跳过素材上传并复用该素材，继续按每批 20 条进入发布队列。
- 素材与发布进度改为独立基线：未发布范围可从已有素材数量开始推进素材进度，而发布进度从该范围内真实已发布数量开始推进。
- 当前任务的第二条进度名称改为“发布成功”，明确数值不代表素材已处理或发布请求已提交。
- 自动发货链路在任务运行时同时展示全局累计数和本轮进度，避免把本轮未发布范围的 `0` 与全局已发布数混淆。

## 2.0.5（2026-07-30）

- 同步任务双进度统一显示“当前已处理游戏数 / 本轮同步范围内的有效游戏总量”，不再使用实际需要发起素材或发布请求的商品数量作为分母。
- 已有商品、已有素材等无需再次请求的游戏会在任务开始时直接计入已处理；任务执行中的素材失败、同名跳过和已有发布记录也会继续推进当前数量。
- 后台任务记录继续保留实际提交发布数和发布处理数，避免改变既有运营统计口径。

## 2.0.4（2026-07-30）

- 看板顶部指标固定为“总游戏数据、有效游戏数据、已同步素材库数据、已发布数据”，异常数据继续在下方任务与日志区域展示。
- 有效游戏数据统一定义为标题、简介、图片链接及至少一个有效 `http/https` 下载链接完整的记录；看板统计与闲鱼同步候选复用同一 SQL 条件。
- 即使商品模板使用固定封面，缺少 Gamer520 图片链接的记录也不会进入同步数据池，确保实际同步只来源于看板的有效数据。

## 2.0.3（2026-07-30）

- 闲鱼同步候选改为只接受至少一个有效 `http/https` 下载地址；没有下载记录、空地址或非网页协议资源会在任务创建前过滤，不占用素材与发布进度。
- 游戏数据详情增加 `games.image_url` 图片链接展示，可直接打开原始图片地址。
- 新增有效下载资源候选和详情图片字段回归测试，完整测试增至 39 项。

## 2.0.2（2026-07-30）

- Docker 构建阶段显式安装 Vite 等开发依赖，完成前端构建后再裁剪为生产依赖，兼容默认生产模式的 Playwright 基础镜像。

## 2.0.1（2026-07-30）

- 补齐 Linux/amd64 镜像构建所需的 Sharp 跨架构可选依赖锁定信息，确保官方 Playwright 基础镜像中的 `npm ci` 可复现安装。

## 2.0.0（2026-07-30）

- 管理界面重构为 React、Tailwind CSS 与 shadcn-ui 风格的正式后台，新增看板、任务、商品配置、游戏数据和 API Key 管理五个独立模块，并完成 390px 移动端适配。
- 新增管理员登录页与签名会话 Cookie，后台数据和操作接口默认需要登录；管理员会话可在 API Key 管理页直接查看服务端配置的闲鱼 Key 与下载源只读 Key 明文。
- 同步任务不再因单件或明确的批次参数失败中断：失败商品记录结果后跳过，继续处理下一件或下一组；鉴权错误和结果未知仍停止，避免无权限重试或重复发布。
- 素材候选在进入远程同步前排除已有闲鱼商品编号、已有本地素材编号或已有发布编号的记录；远程素材库存在同名商品时继续按同名跳过，不重复创建和发布。
- 游戏数据增加单商品同步入口，任务页集中展示定时配置、手动执行、中断恢复、双阶段进度、跳过数量、任务记录和错误日志。
- 新增后台会话、明文 Key 权限边界、单商品同步、既有商品/素材去重及失败继续回归测试，完整测试共 38 项。

## 1.10.2（2026-07-30）

- 闲鱼同步改为每导入 20 个候选商品素材后立即发布当前组，再继续导入下一组，替代等待全部素材导入完成后统一发布。
- 保留 4 路素材并发、首条探针、发布结果核对、卡券关联和安全熔断；探针或批次触发熔断后不会继续导入后续工作组。
- 更新批次交替与探针熔断回归测试，验证 21 条商品按“20 条素材 → 发布 → 1 条素材 → 发布”的顺序执行。

## 1.10.1（2026-07-30）

- 账号商品刷新请求固定使用闲鱼允许的每页 20 条，避免结果核对阶段因分页超限而误触发熔断。
- 新增客户端请求参数回归测试，确保后续发布前后的商品列表刷新保持兼容。

## 1.10.0（2026-07-30）

- 发布前将 Gamer520 当前封面下载、重试、解码校验并统一缓存为 JPEG，素材使用 Gamer520 自身稳定封面地址，避免源站 CDN 偶发超时造成图片上传失败。
- 全量素材导入完成后先提交 1 条探针商品；探针失败、账号异常、同批多条停留发布页、图片失败或批次失败率过高时自动熔断，不再继续产生大批重复失败。
- 每批发布前后强制刷新发布账号商品列表，使用新增商品编号和标题前缀对账；实际已发布但页面未跳转的商品会纠正为成功并继续关联卡券。
- 闲鱼返回成功但缺少商品编号且商品列表无法核验时标记为待确认并停止，避免未知结果自动重发。
- 新增封面缓存、探针熔断和发布结果对账测试，完整测试增至 33 项。

## 1.9.1（2026-07-30）

- 双进度条标题明确调整为“导入素材库进度”和“发布商品进度”，任务摘要使用相同阶段名称。
- 补充页面回归测试，确保两个阶段名称和两条进度条同时渲染。

## 1.9.0（2026-07-30）

- 闲鱼状态统一为“无、加入素材库、发布中、发布成功、更新素材库”，内部失败和待确认状态归入对应生命周期阶段。
- 同步任务调整为严格两阶段：先以 4 路并行完成全部素材库同步，再将待发布商品按每 20 件提交批量发布。
- 同步运行记录新增素材处理数、待发布总数和发布处理数；管理页面增加素材库进度与发布进度两条独立进度条。
- 两阶段进度、任务摘要和任务记录继续展示素材同名跳过数量，数据库升级至版本 9。

## 1.8.1（2026-07-30）

- 采集实时进度和任务记录增加跳过数量，使用已有的 `detail_skipped` 统计。
- 同步实时进度、最近任务摘要和任务记录增加跳过数量，使用已有的 `material_skipped` 统计。
- 同步运行时进度事件增加 `materialSkipped` 字段，页面运行中即可刷新显示。

## 1.8.0（2026-07-29）

- 闲鱼素材同步调整为最多 4 个远程请求并行，SQLite 状态写入仍串行执行；同名标题使用独立顺序锁，避免并发创建重复素材。
- 未发布素材累计到 20 个后提交一次批量发布，最后不足 20 个的尾批也会提交；已发布商品更新仍只更新素材，不进入发布批次。
- 单个素材同步失败不再终止整轮任务，其余成功素材继续发布，并在任务记录中单独统计素材失败数量。
- 同步页面和调度状态展示“4 路素材、20 件/批”，数据库升级至版本 8。

## 1.7.3（2026-07-29）

- 游戏列表的采集状态筛选增加“已更新”，按 `last_change_type=updated` 查看内容发生过更新的商品。
- 增加独立的闲鱼状态筛选，按当前保存的发布账号筛选待同步、素材已同步、已发布、发布中、失败、待确认和同名跳过状态。
- 明确“已更新商品”同步仅更新素材库：商品同步前后都保持已发布，不会重新上架或修改线上闲鱼商品。

## 1.7.2（2026-07-29）

- 采集范围调整为 Gamer520 PC PLAY 热度榜前 50 页，手动采集和定时采集统一生效。
- `PAGE_COUNT` 默认值与硬上限均改为 50，服务器遗留更大配置时也不会采集第 51 页及以后内容。
- 同步更新管理页面提示、Compose 部署配置和使用文档。

## 1.7.1（2026-07-29）

- 采集前批量调用 Gamer520 WordPress `modified_gmt` 接口读取来源更新时间，每次最多处理 100 个文章 ID。
- 来源修改时间不晚于本地最后成功采集时间时直接跳过，不再创建文章页或等待页面完整加载；已变化商品才进入详情与资源解析。
- 接口临时不可用时保留文章页时间回退，并把跳过判定提前到 `DOMContentLoaded`，避免未变化商品等待整页 `load`。

## 1.7.0（2026-07-29）

- 闲鱼同步设置增加标题、简介和图片模板，支持游戏 ID、名称、简介、原图、网盘类型、价格及资源凭据等占位符。
- 管理页面增加模板变量快捷插入和闲鱼商品实时预览，账号、默认价格与三类模板一次保存并持久化到 SQLite。
- 同步素材、封面及卡券关联标题统一使用模板渲染结果；模板变化会重新更新素材，已发布商品不会重复上架。
- 图片模板渲染结果必须为 `http/https` URL；使用自定义固定图片或按 ID 拼接图片时，不再强制依赖 `games.image_url`。
- 数据库升级至版本 7，新增三类模板字段并保持旧配置自动使用原有发布文案。

## 1.6.3（2026-07-29）

- 下载源 POST 接口废弃请求字段 `name`，改为支持 `id`、`item_id` 和 `item_title`，并按 `item_id → item_title → id` 顺序逐级查询。
- `item_title` 精确匹配前会移除全部 `【秒发】` 标记，并兼容标记括号内的多余空格；响应查询策略同步改为 `item_title`。

## 1.6.2（2026-07-29）

- 卡券关联请求增加闲鱼商品标题 `item_title`，标题与实际发布的 `【秒发】` 商品名称一致。
- 下载源 POST 接口允许同时传入 `item_id`、`name` 和 `id`，没有匹配结果时按 `item_id → name → id` 顺序逐级回退，并在响应 `lookup.strategy` 中返回实际命中条件。

## 1.6.1（2026-07-28）

- 修正版本 6 历史进度回填边界：仅对已成功或部分成功的旧同步任务补齐处理数，中断任务保留真实进度，避免部署时把未处理商品误记为完成。

## 1.6.0（2026-07-28）

- 下载源查询接口改为仅接受 `POST` JSON，请求体使用 `id`、`item_id` 或 `name` 三选一；旧 `GET` 请求明确返回 `405`。
- 闲鱼同步改为严格逐商品串行，每次只提交一个素材和一个发布商品，等待结果后再处理下一件。
- 同步页面增加实时进度条、当前商品与处理阶段，任务记录统一展示采集和同步任务，并持久化同步完成数与当前商品。
- 每个发布成功的闲鱼商品自动关联卡券 ID `6`；关联结果写入数据库和任务计数，关联失败可单独重试且不会重复发布商品。
- 数据库升级至版本 6，增加同步进度及卡券关联状态字段。

## 1.5.0（2026-07-28）

- 修复已保存 `account_id` 刷新后下拉框置空：账号列表尚未加载或临时加载失败时，页面仍回显 SQLite 中的当前发布账号。
- 手动同步增加“全部商品”“未发布商品”“已更新商品”三种范围；定时同步可保存执行范围，“全部商品”会重新检查全部有效商品但不会重复发布已成功商品。
- 采集和同步任务增加协作式中断与恢复控制，在列表页、详情项、素材批次和发布批次之间的安全检查点暂停。
- 发布成功后把闲鱼商品编号、链接、账号和发布时间写回 `games`，并自动回填历史成功发布记录。
- 下载源接口增加 `item_id` 查询，可通过闲鱼商品编号返回游戏资源码、解压密码、`data` 字符串和全部下载源。
- 数据库升级至版本 5，调度配置和同步任务记录增加 `sync_mode`。

## 1.4.2（2026-07-28）

- 下载源接口增加顶层字符串字段 `data`：第一行固定输出解压密码，后续每个网盘下载源各占一行，并在存在提取码时同行输出。

## 1.4.1（2026-07-28）

- 下载源接口按名称查询时自动移除开头的 `【秒发】` 或 `【秒发 】` 及相邻空格，再使用原始游戏名称精确匹配。
- 下载源响应增加顶层 `resourceCode`、`archivePassword` 和名称归一化信息，同时保留原有 `game` 与 `downloads` 结构兼容旧调用方。

## 1.4.0（2026-07-28）

- 管理页面增加采集与闲鱼同步的 Cron、时区和启用状态配置，保存后立即重排任务，并分别提供“立即采集”和“立即同步”。
- 增加默认售卖价格和单商品价格覆盖；同步素材时使用当前商品名称、简介、`games.image_url` 封面与实际售价。
- 闲鱼商品介绍根据下载源动态生成“支持网盘”，并追加 24 小时自动发货和咨询提示文案；文案或价格变化会更新素材，已发布商品不会重新上架。
- 保存发布账号时将真实 `account_id` 与默认售价写入 SQLite，刷新页面和服务重启后继续使用已选账号。
- 游戏详情弹窗精简为“游戏凭证、下载源、资源详情页”，移除与发货无关的信息。
- 补充 GitHub 私有仓库安全忽略规则，防止提交环境变量、SQLite 数据库和测试运行产物。

## 1.3.1（2026-07-28）

- 修正闲鱼账号连接鉴权：页面现在使用闲鱼用户管理生成的 `X-API-Key`，不再误用独立的 Gamer520 管理 Key。
- 网页商品详情直接展示下载源，不再弹出只读 Key 输入；对外 `/api/download-sources` 接口继续保留独立只读 Key 保护。
- 闲鱼账号列表、账号保存和手动全量同步统一校验服务端当前 `XIANYU_API_KEY`，确保页面与定时任务使用同一用户权限。

## 1.3.0（2026-07-28）

- 闲鱼同步改为处理全部有效商品，内部仍按每批最多 20 条调用素材和发布接口；素材库名称相同的商品标记为跳过，不再重复创建或发布。
- 采集器记录文章页 `time[datetime]` 的来源更新时间；已有完整详情且来源时间未变化时跳过资源详情采集，只更新本次检查时间。
- 修复无下载源商品打开详情时读取未定义数组导致的异常，并为缺少下载源、封面或来源时间的记录增加稳定空状态。
- 重做闲鱼全量同步区和游戏详情弹窗，移除“本轮数量”，明确展示账号、全量处理流程、同名跳过和分批结果。
- 游戏列表增加来源更新时间展示，同步任务增加同名跳过数和批次数。

## 1.2.0（2026-07-28）

- 采集成功后按标题、简介、`image_url`、资源凭据和排序后的全部下载源计算内容哈希，原子覆盖有效数据并标记“新增”或“更新”。
- 增加闲鱼素材同步、按账号发布结果、同步批次和单例账号设置；每轮最多处理 20 条带下载源、标题、简介和封面的商品。
- 新商品以 `【秒发】` 前缀、固定 1 元和 `games.image_url` 唯一封面创建素材并发布；已发布商品更新时仅更新素材。
- 管理页面增加闲鱼账号加载、选择保存、切换确认、立即同步和最近同步状态。
- 增加受管理 Key 保护的账号与同步接口，以及独立只读 Key 保护的按 ID 或精确名称查询下载源接口。
- 自动同步默认每 6 小时执行；采集和同步互斥，冲突任务在当前任务结束后补跑。

## Cloudflare 路由 1.0.1（2026-07-28）

- 将采集观测台域名从 `game520.xyyamsz.cn` 更正为 `gamer520.xyyamsz.cn`。
- 保持源站、SSH 路由和未匹配请求的 404 兜底不变。

## Cloudflare 路由 1.0.0（2026-07-28）

- 新增 `game520.xyyamsz.cn` Cloudflare Tunnel 路由。
- 保留既有 `ssh.xyyamsz.cn` SSH 路由及未匹配请求的 404 兜底。
- 将源站限制为服务器回环地址 `127.0.0.1:13520`。
